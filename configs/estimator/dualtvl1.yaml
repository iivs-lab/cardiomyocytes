# Dual TV-L1, which cv2 implements for CPU and CUDA. Slower than Farneback and
# the only one of the three that answers identical frames with exactly zero.
_target_: iivs_cardio.optical_flow.estimators.opencv.dualtvl1.DualTVL1Config
