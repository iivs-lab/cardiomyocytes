# @package _global_

defaults:
  # The filter, defaulting to a 2x2x2 ellipsoid median. Swap it with
  #   filter=<option>
  # naming any file in the group, `identity` among them for no filtering.
  - /filter@filter: median_ellipsoid_2x2x2
  - _self_

data:
  root: ??? # required: the folder to search for phase .bin sequences