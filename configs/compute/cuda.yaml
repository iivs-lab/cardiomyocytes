device: cuda
gpu_ids: [0] # one worker per id, null every visible device
progress_bar: true
insights: false # per-worker timings, printed once a pool finishes
worker_lifespan: null # tasks before a worker is replaced; null keeps it for the run
