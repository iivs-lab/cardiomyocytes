device: cuda
gpu_ids: [0] # one worker per id, null every visible device
progress_bar: true
log_insights: false # how busy each worker was, logged once a pool finishes
lifespan: null # tasks before a worker is replaced; null keeps it for the run
